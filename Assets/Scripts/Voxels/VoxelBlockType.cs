namespace SCoL.Voxels
{
    // Keep it tiny for now; you can swap these to your custom block models later.
    public enum VoxelBlockType : byte
    {
        Air = 0,
        Grass = 1,
        Dirt = 2,
        Stone = 3,
        Water = 4,
    }
}
